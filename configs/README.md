# Configuration Files

This directory contains sanitized copies of configuration files used throughout the homelab.

Sensitive information such as passwords, private keys, API tokens, and personal identifiers has been replaced with placeholders before being committed to the repository.

These files are intended as templates and documentation rather than exact backups of the running system.
