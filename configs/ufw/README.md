# UFW Configuration

Current firewall configuration:

* UFW enabled
* OpenSSH allowed
* Default incoming policy: deny
* Default outgoing policy: allow

Additional firewall rules will be documented here as services are deployed.
